const path = require('path');

module.exports = {
    config: path.resolve(__dirname, 'backend', 'src', 'inhouse_bot', 'configs', 'database.js'),
    'migrations-path': path.resolve(__dirname, 'backend', 'src', 'inhouse_bot','database', 'migrations')
}